import CountryLabel from '../select/src/countryLabel';

/* istanbul ignore next */
CountryLabel.install = function(Vue) {
  Vue.component(CountryLabel.name, CountryLabel);
};

export default CountryLabel;
